/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    turbo: {
      rules: {
        // Turbopack rules for 2026 stack
      }
    }
  }
};

module.exports = nextConfig;
